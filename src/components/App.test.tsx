import React from "react";
import { render } from "@testing-library/react";
import App from "./App";

test("renders learn react link", () => {
    // App should resnder without throwing an error
    render(<App />);
});
